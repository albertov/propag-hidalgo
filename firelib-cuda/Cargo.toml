[package]
name = "firelib-cuda"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cuda_std = "*"
firelib-rs = { path = "../firelib-rs", default-features = false }

[build-dependencies]
cuda_builder = "*"
