[package]
name = "firelib-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
const_soft_float = { git = "https://github.com/albertov/const_soft_float.git", features= ["no_std"]}
uom = { version = "0.36.0", default-features = false, features=["f64", "si", "serde"] }
cuda_std = { git = "https://github.com/Rust-GPU/Rust-CUDA/", optional=true }

[dev-dependencies]
firelib-sys = { path = "../firelib-sys" }
quickcheck = "1.0.3"
rand = "0.9.0"

[features]
default = []
std = []
cuda = ["dep:cuda_std"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(test)'] }
